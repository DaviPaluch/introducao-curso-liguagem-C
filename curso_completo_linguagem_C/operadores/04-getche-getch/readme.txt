A biblioteca de funções C oferece duas funções que lêem um caractere no instante em que é digitado 
sem esperar pela tecla ENTER;

getche() == retorna um caractere lido no teclado ;

getch() == retorna um caractere lido no teclado sem ecoar no vídeo;
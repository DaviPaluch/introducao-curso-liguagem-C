OUtras duas funções da que tem o protótipo definido no arquivo stdio.h
é a função getchar();

getchar() == função que aguarda o caractere de entrada(teclado) e retorna o caratere lido.

putchar() == função que imprime um caractere na saida-padrão(vídeo)